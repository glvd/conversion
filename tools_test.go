package conversion

import "testing"

func TestInitFFTool(t *testing.T) {
	tests := []struct {
		name string
	}{
		{
			name: "fftool",
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
		})
	}
}
